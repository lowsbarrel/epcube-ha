# Home Assistant does not allow a `target:` device filter on a service, so the
# device is an ordinary field carrying a device selector instead.

set_tou_schedule:
  fields:
    device_id:
      required: true
      selector:
        device:
          integration: epcube
    peak:
      required: false
      example: '[{start: "08:00", end: "12:00", price: 0.31}]'
      selector:
        object:
    mid_peak:
      required: false
      example: '[{start: "12:00", end: "18:00", price: 0.22}]'
      selector:
        object:
    off_peak:
      required: false
      example: '[{start: "22:00", end: "08:00", price: 0.12}]'
      selector:
        object:
    peak_non_workday:
      required: false
      selector:
        object:
    mid_peak_non_workday:
      required: false
      selector:
        object:
    off_peak_non_workday:
      required: false
      selector:
        object:
    apply:
      required: false
      default: false
      selector:
        boolean:

set_operating_mode:
  fields:
    device_id:
      required: true
      selector:
        device:
          integration: epcube
    mode:
      required: true
      selector:
        select:
          translation_key: operating_mode
          options:
            - self_consumption
            - time_of_use
            - backup
    reserve_soc:
      required: false
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "%"
          mode: slider

force_charge:
  fields:
    device_id:
      required: true
      selector:
        device:
          integration: epcube
    target_soc:
      required: true
      example: 80
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "%"
          mode: slider
    duration:
      required: false
      example: "02:00:00"
      selector:
        duration:

force_discharge:
  fields:
    device_id:
      required: true
      selector:
        device:
          integration: epcube
    target_soc:
      required: true
      example: 30
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "%"
          mode: slider
    duration:
      required: false
      example: "02:00:00"
      selector:
        duration:

hold_battery:
  fields:
    device_id:
      required: true
      selector:
        device:
          integration: epcube
    duration:
      required: false
      example: "04:00:00"
      selector:
        duration:

clear_override:
  fields:
    device_id:
      required: true
      selector:
        device:
          integration: epcube
